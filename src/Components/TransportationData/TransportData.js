import React from "react";

const TransportData = () => {
    const key = 'abd31ca55f920bd7960baad553c870a3';
    let url = `http://api.aviationstack.com/v1/flights?access_key=${key}`;
    // fetch(url).then(res => res.json()).then(cslg);
    return (
        <React.Fragment>

        </React.Fragment>
    );
}

export default TransportData;